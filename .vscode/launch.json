{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Dashboard in Browser",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}/dash/static",
            "preLaunchTask": "Start HTTP Server",
            "postDebugTask": "Stop HTTP Server"
        }
    ]
}
